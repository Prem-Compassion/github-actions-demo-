name: 03-workflow-runner
on: push

jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps: 
            - name: show os
              run: |
                echo "This job is running on an ubuntu OS"
                echo "Runner OS : $RUNNER_OS"
                
